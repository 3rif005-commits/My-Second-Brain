# Components Workspace

Prototype individual UI components here before wiring them into the main app.
Each component gets its own `.tsx` file. Can be copy-pasted into `frontend/components/`
when ready.
